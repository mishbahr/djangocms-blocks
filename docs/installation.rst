============
Installation
============

At the command line::

    $ easy_install djangocms-blocks

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv djangocms-blocks
    $ pip install djangocms-blocks